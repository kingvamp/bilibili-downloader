# CONTEXT

## 当前正在做什么
- 修改视频下载文件名格式，增加 UP 主名称和投稿时间前缀
- 完成在设置界面增加下载记录条目数量显示

## 上次停在哪个位置
- 已实现主进程 IPC、预加载脚本 API 及渲染进程 UI 显示，并同步了类型定义。

## 近期的关键决定和原因
- **严格区分大小写**：移除了代码中所有针对 BV 号的 `toUpperCase()` 和 `/i` 正则标志，因为 BV 号协议本身是区分大小写的，以前的模糊处理导致了 ID 破坏和比对失效。

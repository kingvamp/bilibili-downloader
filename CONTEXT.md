# CONTEXT

## 当前正在做什么
- 已完成 BV 号大小写敏感性问题的检查与修复。

## 上次停在哪个位置
- 修复了 `bilibili.ts`, `api.ts`, `downloader.ts` 中的逻辑。
- 验证了 Base58 编解码的大小写敏感性及前缀校验。

## 近期的关键决定和原因
- **严格区分大小写**：移除了代码中所有针对 BV 号的 `toUpperCase()` 和 `/i` 正则标志，因为 BV 号协议本身是区分大小写的，以前的模糊处理导致了 ID 破坏和比对失效。
